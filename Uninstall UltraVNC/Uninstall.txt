$uninstallPath = "C:\Program Files\uvnc bvba\UltraVNC\unins000.exe"

if (Test-Path $uninstallPath) {
    Write-Output "Found unins000.exe at $uninstallPath. Running it now..."
    
    Start-Process -FilePath $uninstallPath -ArgumentList "/silent" -NoNewWindow -Wait
    
    Write-Output "Uninstall command sent to unins000.exe."
} else {
    Write-Output "Malwarebytes unins000.exe not found at the specified path."
}